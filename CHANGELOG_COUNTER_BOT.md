# 计数监听机器人更新记录

版本由 `VERSION_COUNTER_BOT` 记录。本文件只记录云端计数监听机器人的发布，不改变本地互推检查脚本的版本。

## v3.11.3 - 2026-09-13

- 修复违规回复已发成功但 GitHub 状态上传失败后，下轮读取旧状态再次回复的问题。完整群快照核对机器人真实身份、原消息ID和现有违规文案，从已发群回复恢复去重记录，不依赖上轮状态上传成功。
- 普通违规回复统一等本轮群消息回执核验后发送；未核验、核验过期或核验失败时不盲目补发，已确认的相同原消息与违规原因不再重复回复。原失败重试和19:00后静默规则保留。
- GitHub 状态上传对临时故障最多重试三次，不强推、不覆盖远端；不新增后台规则，不改文案、资格判断、定时频率和互推检查逻辑。

## v3.11.2 - 2026-09-13

- 修复普通违规回复在 Telegram 发送失败前已记录为成功的问题；只有接口确认成功才去重，失败保留重试状态，并记录接口具体错误而不泄漏令牌。
- 用户会话完整补齐当日群消息后，沿用现有“粉丝数量不足”和“缺少社区账号”的开关、文案、资格规则补发漏回消息；主推号或回推号任一粉丝合格仍不发粉丝不足回复。
- 回复状态独立于每日计数快照保存，成功不重复，失败间隔重试；发送前核对北京时间，19:00起不补发普通违规回复，已入选换帖仍由专用回执处理。
- 不新增后台控件，不修改文案配置、粉丝门槛、互推检查、自动转帖或定时计划。

## v3.11.1 - 2026-09-13

- 修复云端用户会话补齐群消息未启用第二条回推主页链接解析的问题：群二、群三与实时收录使用同一个开关，第二条可为帖子或主页链接。
- 修复旧快照已漏掉回推主页时因文本未改而永久复用的问题；发现缺失的回推绑定后重新解析，正确快照仍复用，不重复请求X。
- 维持原粉丝规则：主推号或回推号任一达到群门槛即可，不把粉丝相加。只第一条主推链接进入名单并检查指定@；群一回推主页开关、各群门槛、时间规则和其他功能均不变。
- 新增完整收录路径、旧缓存修复、精确门槛、主链接文案和分群隔离回归测试。

## v3.11.0 - 2026-09-13

- 北京时间19:00前完整群快照确认消息已删除或已删去帖子链接时，记录退出证据；从已发私信名单移除该消息，并按同群原始发帖顺序补入下一条合格候选，不采用回推链接或跨群数据。
- 没有合格候选时减少人数保留空位；截止前后续出现合格候选可补足原名额，不因改大上限自动扩张已发名单。成员变化后仍按较晚发帖在上、较早发帖在下编号，保留已有换帖标记。
- 私信编辑失败保存待同步名单，保留原成功状态供重试；19:00后不作新补位，但重试此前已确定的名单更新。同成员合格换帖和19:00后换帖回执静默规则不变。
- 群快照原子替换，读取/解析不完整时不覆盖旧登记、不判退出；按消息ID查询必须返回完整匹配结果才允许确认删除。
- 不修改手机检查、自动转帖、群内文案、上限配置或定时计划。

## v3.10.1 - 2026-09-13

- 已发私信名单换帖成功并保存确认状态后，在原群消息下回复“更换的新链接已收录”；私信未更新成功时不回复收录成功。
- 已入选成员的新帖子被现有规则确认违规时，确认私信仍保留旧链接后回复“此编辑后的新链接违规，不予收录，已收录原本旧链接”；不再叠加原来的通用违规回复。
- 两种换帖回执均在北京时间19:00至23:59停止；每条发送前重新核对时间，私信名单编辑同步仍照常运行，不跨日补发旧回执。
- 原群消息、账号、发送者和日期校验与私信编辑共用；同次换帖结果去重，群回复失败只重试回执，不重新发送整份名单。
- 新增同步确认、失败重试、时间边界、分群隔离等回归；不修改本地后台v3.9.0、互推检查、自动转帖、既有群内文案配置和定时计划。

## v3.10.0 - 2026-09-12

- 私信名单首次发送后固定原成员、原群消息及编号，并保存 Telegram 消息ID；原消息内同一主推账号换成合格帖子时，直接编辑原私信，在对应编号前标注“已编辑”。
- 满额和19:00之后继续同步当日原名单的编辑；不补入新成员，不采用回推链接，不更改人数、顺序和其他资格规则；新帖不合格时保留旧链接。
- 编辑失败保留旧状态供下轮重试；相同内容不重复编辑；群间、跨日及来源不明的修改不套用。
- 旧私信没有消息ID时默认保持不变；可在手动运行中明确指定当天日期及一个群，补发一次可编辑的版本，旧消息不删除。
- 增加编辑同步、名额锁定、来源隔离、失败重试和旧消息迁移回归；不修改互推检查、自动转帖、群回复文案和定时计划。

## v3.9.2 - 2026-09-10

- 名额及候选提醒按原消息的北京时间筛选；当日19:00前的合格链接，任务延迟到19:00后仍可补回复，成功后不重复发送。
- 19:00及之后发送的链接仍不触发名额回复；不跨日补发旧消息，未完成群快照不回复。
- 保留发送失败重试、分群开关、自定义文案和候选每增加3条的阶段规则；不修改名单筛选、私信发送、定时任务或其他违规回复。

## v3.9.1 - 2026-09-09

- 修复名额及私信测试读取正式后台名额配置的问题，测试使用独立名额数据；调整群名额不再导致固定测试断言失败。
- 增加不限额、1条、30条、40条、500条配置的回归，验证正式逻辑仍按后台配置处理名单和候选回复。
- 不修改正式名额、群规则、定时时间、收录筛选、私信格式或发送去重逻辑。
