# 计数监听机器人更新记录

版本由 `VERSION_COUNTER_BOT` 记录。本文件只记录云端计数监听机器人的发布，不改变本地互推检查脚本的版本。

## v3.9.2 - 2026-09-10

- 名额及候选提醒按原消息的北京时间筛选；当日19:00前的合格链接，任务延迟到19:00后仍可补回复，成功后不重复发送。
- 19:00及之后发送的链接仍不触发名额回复；不跨日补发旧消息，未完成群快照不回复。
- 保留发送失败重试、分群开关、自定义文案和候选每增加3条的阶段规则；不修改名单筛选、私信发送、定时任务或其他违规回复。

## v3.9.1 - 2026-09-09

- 修复名额及私信测试读取正式后台名额配置的问题，测试使用独立名额数据；调整群名额不再导致固定测试断言失败。
- 增加不限额、1条、30条、40条、500条配置的回归，验证正式逻辑仍按后台配置处理名单和候选回复。
- 不修改正式名额、群规则、定时时间、收录筛选、私信格式或发送去重逻辑。
